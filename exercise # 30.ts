let usernames: string[] = ["admin’, ‘Eric’, ‘John’, ‘Alice’, ‘Mike"];
for (let i = 0; i < usernames.length; i++) {}  if (usernames[1] === "admin") {console.log("Hello admin, would you like to see a status report?");}
else{    console.log(`Hello ${usernames[1]}, thank you for logging in again.`);}
