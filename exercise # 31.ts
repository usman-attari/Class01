let Usernames: string[] = ["admin’, ‘Eric’, ‘John’, ‘Alice’, ‘Mike"];
if (usernames.length === 0) {console.log("We need to find some users!");}
else{for (let i = 0; i < usernames.length; i++)}
if (usernames[1] === "admin"){console.log("ello admin, would you like to see a status report?");}
else{    console.log(`Hello ${usernames[1]}, thank you for logging in again.`);} {}
usernames = [];
    if (usernames.length === 0) 
      console.log("We need to find some users!");
    